sudo: required

services:
    - docker

before_install:
    -  docker build -t road_recognizer_docker -f Dockerfile .

script:
    - docker run --rm road_recognizer_docker bash -c "catkin_make run_tests && catkin_test_results"
